#pragma shader_stage(vertex)

vec2 positions[3] = vec2[]
(
	vec2(-1,-1),
	vec2(3,-1),
	vec2(-1,3)
);

layout(location = 0) out vec2 texcoord;

void main()
{
	vec2 pos = positions[tz_VertexID];
	gl_Position = vec4(pos, 0.0, 1.0);
	texcoord = 0.5 * pos + vec2(0.5);
}
